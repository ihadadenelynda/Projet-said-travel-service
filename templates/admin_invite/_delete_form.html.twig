<form method="post" action="{{ path('app_admin_invite_delete', {'id': invite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invite.id) }}">
    <button class="btn">Supprimer</button>
</form>
