{% extends 'base.html.twig' %}

{% block title %}Invite{% endblock %}

{% block body %}
    <h1>Invite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invite.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ invite.firstName }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ invite.lastName }}</td>
            </tr>
            <tr>
                <th>Date et heure</th>
                <td>{{ invite.appointmentTime| date('y-m-d h:i')}}</td>
            </tr>
        </tbody>
    </table>

    <button><a href="{{ path('app_admin_invite_index') }}">Retour</a></button>

   <button> <a href="{{ path('app_admin_invite_edit', {'id': invite.id}) }}">Modifier</a></button>

    {{ include('admin_invite/_delete_form.html.twig') }}
{% endblock %}
